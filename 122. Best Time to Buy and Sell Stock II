class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int max_profit = 0;
        if(prices.size() == 0){
            return max_profit;
        }
        for(int i = 0; i + 1 < prices.size(); i ++){
            if(prices[i + 1] - prices[i] > 0){
                max_profit += prices[i + 1] - prices[i];
            }
        }
        return max_profit;
    }
};

//don't forget line 5-7
