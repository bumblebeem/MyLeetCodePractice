class Solution {
public:
    bool canJump(vector<int>& nums) {
        if(nums.size() == 0){
            return false;
        }
        int last = nums[nums.size() - 1];
        for(int i = nums.size() - 1; i >= 0; i --){
            if(i + nums[i] >= last){
                last = i;
            }
        }
        return last == 0;
    }
};

//greedy还处于不太会的阶段啊。。。
